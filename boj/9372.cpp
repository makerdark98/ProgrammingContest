#include <iostream>
int main () {
    int T;
    scanf("%d", &T);
    while(T--) {
        int n, a, tmp;
        scanf("%d%d", &n, &a);
        for(int i=0;i<a;i++){
            scanf("%d", &tmp);
            scanf("%d", &tmp);
        }
        printf("%d\n", n-1);
    }
    return 0;
}
