#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
using namespace std;
#define sci(n) scanf("%d", &(n))
#define MAX_SIZE 100010
#define MAX_NUMBER 1000010

int a[MAX_SIZE];
void getPrimeDivisor(int t, vector<int>& retval);
int main () {
  int n;
  sci(n);
  for(int i=0;i<n;i++)
    sci(a[i]);

  for
  return 0;
}
